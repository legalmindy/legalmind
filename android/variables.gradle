ext {
    // NOTE: org.apache.cordova:framework 14.0.1 (pulled in automatically by Capacitor's
    // cordova-android bridge, even though this app uses zero actual Cordova plugins) hard-requires
    // minSdkVersion 24 — Gradle's manifest merger rejects anything lower with a hard build error.
    // 24 (Android 7.0, 2016) already covers ~99% of active Android devices, so this is kept as the
    // real, verified floor instead of forcing an override that could cause runtime crashes on API 21-23.
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}